name: Image Link Check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  image-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build website (render hook validates images)
        run: docker compose run --rm hugo hugo --minify
